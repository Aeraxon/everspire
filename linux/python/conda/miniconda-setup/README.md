# Conda

Miniconda Installation und Environment-Management.

## Scripts

### miniconda-install.sh
Installiert Miniconda mit Python 3.12 in `~/miniconda3`.

```bash
./miniconda-install.sh
```

### init-conda.sh
Initialisiert Conda für verschiedene Shells (bash, zsh, fish).

```bash
./init-conda.sh
```
